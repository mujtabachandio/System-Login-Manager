{
  "name": "inquirer-in-typscript",
  "version": "1.0.0",
  "description": "inquirer not working soo now im using prompt-sync",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mujtaba chandio",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.11.2",
    "prompt-sync": "^4.2.0"
  },
  "devDependencies": {
    "@types/prompt-sync": "^4.2.3"
  }
}
